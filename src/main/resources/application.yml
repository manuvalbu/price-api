server:
  port: 8888

spring:
  datasource:
    driverClassName: org.h2.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: jdbc:h2:mem:PriceDatabase
  h2:
    console.enabled: true
    console.path: /h2-console

rest:
  base-url: /api/inditex
  endpoints:
    price: /price
